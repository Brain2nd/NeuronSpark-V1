# V4.1 量子化释放 (quantal) + AHP 实验日志

**起始日期**: 2026-05-11
**分支**: `v4`
**架构基线**: V4 = v3 删 conv1d + token-level 残差流 + segmented PLIF（携带第 k_t 帧）+ output_neuron k_predictor + Triton fused kernels

本文记录 V4.1 神经元设计（量子化释放 spike form + 后超极化 AHP）从设计到实测的全过程，所有 commit hash / 日志路径都列在每节末尾，为后续写论文提供原始数据。

---

## §1 设计背景与决策

### 1.1 起点：v3 现状 (supra-threshold release)
- `output = relu(V_pre - v_th)`，软重置 `V_post = min(V_pre, v_th)`
- 反向梯度精确 ReLU：`∂output/∂V_pre = 𝟙[V_pre>v_th]`，无 surrogate
- 问题：发放时膜被钉回 v_th，**「累积了多强的上下文」从膜里被抹掉**，只活在 output 流里

### 1.2 设计讨论（与作者）
- 用户原则：「**历史膜电位承载历史上下文信息**，硬重置不可讨论」
- 候选：(A) supra 现状 / (B) quantal 量子化 (output=v_th·s, 余量留膜里) / (C) 硬重置（OFF the table）/ (D) 分数释放
- 用户决策：**改为 quantal**，AHP（后超极化 / 不应期）一同实现
- 用户初始问：「AHP 会不会引起梯度消失？」
- 我（错误地）笃定答：「不会，supra 下 `∂V_post/∂V_pre=1-s_hard` 不变」—— 但只在 supra 验证了，没在 quantal 重做数学；后果见 §3.4

### 1.3 量子化释放正式定义
```
spike      = 𝟙[V_pre > v_th]                    (硬指示子, forward 用)
output     = v_th · spike                        (固定一份 v_th, 矩阵-向量层面)
V_post     = V_pre - v_th·s - ahp·s              (软重置: 余量 V_pre-v_th 留膜; AHP 额外下压)
反向 (surrogate, sigmoid'(α·x))：
  ∂out/∂V_pre   = v_th · α·σ·(1-σ) ≈ v_th·α/4 ≈ 0.02   ← **比 supra 弱 ~50x**
  ∂V_post/∂V_pre = 1 - (v_th + ahp) · α·σ·(1-σ)
```
这个 ~50x 弱梯度是后续 quantal 训练困难的核心源头。

---

## §2 实现 (commit 序列)

### 2.1 commit `255d1ff` — quantal + AHP + 配置 (2026-05-11)
- `NeuronSparkConfig`: `spike_mode ∈ {"supra","quantal"}`, `use_ahp`, `ahp_init`, `surrogate_alpha`
- `modeling_neuronspark.py`:
  - 模块级 `NEURON_SPIKE_MODE/USE_AHP/...`，由 `SNNLanguageModel.__init__` 从 config 设置
  - `PLIFNode`/`SelectivePLIFNode` 据此创建 per-channel `ahp` 参数
  - `_spike_step()` 统一 PyTorch 实现（straight-through `s = sig + (s_hard - sig).detach()`）
- Triton 内核：6 个修改（fwd/bwd × rowparam/selective）加 `SPIKE_QUANTAL: tl.constexpr` + `ALPHA` runtime + `AHP_ROW_ptr` + `GRAD_AHP_ROW_ptr`
- 覆盖 6 处 segmented PLIF 调用点
- 测试：fp64 gradcheck (supra+AHP), kernel vs reference (quantal+AHP, supra+AHP) —— 全过
- e2e overfit smoke 4 配置 (D=64×4 层小模型)：loss 下降、finite、no NaN

### 2.2 commit `1e6845e` — ablation 脚本 (2026-05-11)
- `scripts/v4_neuron_ablation.py`: 4 配置 {supra/supra+ahp/quantal/quantal+ahp}
- 支持 parquet 或 binned `.bin` 数据
- per-layer hidden firing rate logging

### 2.3 commit `a6e3376` — Muon+Adam+Lion 混合优化器 (2026-05-11)
- `utils/muon_adam_lion.py`: `MoonshotMuonAdamLion` (distributed, ZeRO-0 兼容) + `SingleDeviceMoonshotMuonAdamLion`
- 三种 aux 路径：Muon (matrices) / Adam (embed/norm) / **Lion (neuron scalars)**
- Lion update: `lr · sign(β1·m + (1-β1)·g)`，scale-invariant，处理稀疏小幅梯度
- `build_muon_adam_lion_param_groups()`: 4 组 → 后改为 5 组（commit `57b0783`）

### 2.4 commit `57b0783` — clamp v_th/ahp ≥ 1e-4 (2026-05-11)
- Lion update 后 `p.clamp_(min=1e-4)` 防 v_th/ahp 漂负数翻转动力学
- 拆 Lion neuron 为两组：`neuron-pos` (v_th, ahp, 带 clamp) + `neuron-free` (w, b_*, 无 clamp，因 β=sigmoid(w) 需 w 可负)

### 2.5 commit `0a50df8` — LSUV-style 初始化 scaffold (WIP, 2026-05-11)
- `utils/lsuv_snn_init.py`: forward sample → reconstruct V_pre → set v_th = (1-target_p_fire) per-channel quantile
- **当前不解决 quantal 级联**：quantile-init 反而把 v_th 设小、output 幅度更小、cascade 更严重
- 下一版应用 `v_th = c · std(V_pre)` (c≈0.5)，最大化 output 幅度

### 2.6 commit `de2005e` — NaN 诊断脚本 (2026-05-11)
- `scripts/v4_diag_nan.py`：每步检查 loss/grad/param finite；forward hook 找第一个 NaN 张量；触发时 dump param/grad norms + firing rate trajectory + 最近 20 步轨迹

---

## §3 实验记录

### §3.1 实验 1: Adam baseline (4 配置 × 1500 步)
**目的**：验证 v4.1 实现是否正常训练，给后续做对比基线。

**配置**：D=512, N=16, K=12, 12 层, D_ff=1024, memory_layer_interval=4, ~290M 参数；`sft_think_binned_2048` shard 0 切 2400 docs × 512 seq；batch 4；Adam lr=2e-3；1500 步；4 卡并行（每卡一个 config）。

**最终结果（last-100 smoothed loss + 终态发放率）**:

| 配置 | final_loss | hidden 发放 (所有层) | gate0 (FFN, layer 0) | 备注 |
|---|---|---|---|---|
| supra | 7.328 | μ0.000（全死） | 0.470（健康）| baseline |
| supra+AHP | 7.131 | μ0.000（全死）| 0.496（健康）| AHP 帮 -0.20 |
| **quantal** | **6.434** | μ0.000（全死） | **0.000（也死）** | hidden 从 step 0 就 0.006，整轮 0 |
| quantal+AHP | 6.609 | μ0.000（全死） | 0.050（半死）| AHP 在 quantal 下害 +0.18 |

**关键观察**：
1. **hidden 神经元 4 个配置全塌**（supra 起点 0.30 → step 400 起塌；quantal 从 step 0 就 ~0.006）
2. **quantal 把 FFN gate 也搞死了**（supra 0.47 vs quantal 0.00）
3. quantal loss 反而最低 —— 因为 SNN 块和 FFN gate 都死，模型退化成「线性残差 + attention」，简单结构在 1500 步小数据里训得快
4. **不能用此对比判断 quantal 优劣** —— 比的是僵尸的颜色

**根因分析**：
- supra hidden 塌：1500 步 / 单 shard / 100 万 token 太少，SNN 块的复杂多步递推动力学没被推起来；模型走 W_skip + attn + FFN 路径就够用了。**注意**: 不是「SNN 不重要」，是「这点训练量推不动这个复杂的 recurrent 神经元」
- quantal FFN 塌：quantal 输出 `v_th·s ≈ 0.02` 太小，downstream FFN 几乎收不到信号

**日志**: 4090 `/workspace/abl_{supra,supra_ahp,quantal,quantal_ahp}.log`（Adam baseline 1500 步全完成）

### §3.2 实验 2: 本地 v_th 缩放探针（D=384 小模型）
**目的**：探查「初始化要不要给 quantal 单独缩放 v_th」。

**配置**：D=384, N=8, K=8, 8 层；D_ff=768；7 个配置；30 步 short overfit + 前后发放率。

| 配置 | hidden μ init | hidden μ @30步 | loss 30步 |
|---|---|---|---|
| supra 基准 | 0.40 | 0.19 | 11.79→9.14 |
| **quantal 原样** | 0.22 | **0.32（回升）** | 11.75→**9.08** |
| quantal v_th×5 vmin0.1 | 0.24 | 0.25 | 11.78→9.13 |
| quantal v_th×10 vmin0.2 | 0.22 | 0.24 (gate 掉到 0.07) | 11.82→8.90 |
| quantal v_th×10 vmin0.3 | 0.21 | 0.23 | 11.82→8.91 |
| **quantal v_th×20 vmin0.5** | 0.21 | **NaN @ step1** | — |

**结论**：v_th 缩放 ×5 ~ ×10 不解决问题，反而抑制 gate（FFN gate 的 v_th 也被一并缩大）；×20 直接 NaN。**小模型里 quantal 原样跟 supra 打平**。

→ 「怎么初始化」直接结论：**保持现有初始化，不给 quantal 单独缩放 v_th**。

但 §3.1 大模型 quantal hidden 是 0.006，跟小模型的 0.22 不一致 —— **模型尺寸效应**。详见 §4。

### §3.3 实验 3: Muon+Adam+Lion (MAL) ablation
**目的**：测试 Lion 是否能让稀疏 SNN 梯度（quantal 50x 弱 + 时间稀疏）下 SNN 学起来。

**实现**：commit `a6e3376` 的 MoonshotMuonAdamLion；默认 muon_lr=0.02、adam_base_lr=2e-4、lion_lr=5e-4×neuron_mult=10 → 神经元 LR=5e-3。

**4090 第一次 run（默认 LR）**：
- **supra**: step 200 hidden μ0.241 (Adam 同期 0.001-0.010！), loss 5.55
- **supra+AHP**: step 200 hidden μ0.231, loss 5.74
- **quantal**: NaN @ step 94
- **quantal+AHP**: NaN @ step 71

**关键观察**：
1. **Lion 把 supra 的 SNN 救活了** —— hidden 不再随训练塌掉。Adam baseline 同模型 step 400-600 hidden=0.000；MAL step 600 hidden μ0.196 健康
2. **quantal 在 MAL 下激活了 hidden（μ0.006→0.131 over 200 步）但很快 NaN**

### §3.4 quantal NaN 调查记录
诊断假设 → 验证 → 修复尝试，按时间顺序：

**假设 A: Lion LR 太大，v_th/ahp 漂到负数引发翻转**
- 修：lion_lr 5e-4 → 1e-4 (neuron_mult 10→1.0)
- 结果：
  - quantal NaN @ step 216（之前 94，撑久了）
  - quantal+AHP NaN @ step 173（之前 71，撑久了）
- 部分成立，但还是 NaN。

**假设 A 进一步降 LR**：
- 修：quantal+AHP lion_lr → 5e-5
- 结果：NaN @ step 67 —— 反而更早，**否定纯 LR 假设**

**假设 B: v_th/ahp 漂负是必要条件，optimizer 加 clamp_(min=1e-4) 防翻转**
- 修：commit `57b0783`，Lion update 后对 v_th/ahp 钳到 ≥ 1e-4；拆 Lion 为 neuron-pos / neuron-free
- 结果：quantal NaN @ step 116, quantal+AHP NaN @ step 111 —— **否定 v_th/ahp 翻转假设**

**当前状态（实验中）**：commit `de2005e` 上的 NaN 诊断脚本在 4090 GPU2 跑 quantal 模式，加 forward/backward hook 精确定位第一个 NaN 张量。

**诊断结果（quantal-no-AHP, muon_lr=0.02, lion_lr=1e-4, NaN @ step 157）**:
- **NaN 是反向梯度溢出，不是前向爆炸** —— forward hooks 显示所有 layer 输出 finite，loss=5.41 finite。backward 触发第一个 NaN grad 在 `snn.embed_tokens.weight` (随后所有层 norm/v_th 的 grad 都 inf)
- **罪魁是 Muon 把 matrix 权重越推越大**：

  | 量 | step 0 | step 150 | 倍数 |
  |---|---|---|---|
  | W_in max-abs | 3.6e-2 | 3.83 | **100x** |
  | W_out max-abs | 6.1e-2 | 3.34 | **55x** |
  | W_th_x max-abs | ~0.4 | 5.22 | ~13x |
  | gnorm | 0.27 | 0.99（每步顶死 clip=1.0）| 撞顶 |
  | hidden_fr_μ | 0.006 | 0.12-0.13 | 救活了 |
  | v_th min | 0.0074 | 0.0001（撞 clamp_min）| 钳住了 |

- **机制定量**：Moonshot scaling = `0.2 · √max(rows,cols) = 0.2·√8192 ≈ 18.1`。每步 W_in 元素更新 RMS = `muon_lr · 18.1 = 0.02·18.1 = 0.36`。150 步 × momentum 平滑 → 元素从 ~0.04 长到 ~3.8。**完全符合 Moonshot 公式**。
- **为什么 supra 能用同 LR 不爆**：supra 输出 `relu(V_pre - v_th)` 随 V_pre 同步缩放 → 系统自平衡，W_in 长不会失控。quantal 输出锁死在 v_th（~0.02-0.25），W_in 大 → V_pre 大但 output 还是小 → W_out 为了把小信号放成 logits 不得不一直长 → 反向梯度链 `(∂loss/∂out) · output · activation^T` 在 bf16 下溢出 → NaN。

**确诊**：quantal 下 Muon LR 0.02 太大。supra 不爆是因为 supra 的输出对 V_pre 弹性吸收 W_in 增长。**修复路径**：
1. 降 muon_lr 到 0.005（4x）—— 简单一步实验。
2. 或矩阵改 Adam（保留 Lion 给神经元参数）—— quantal-only 路径。
3. 加 weight decay 给 Muon。
4. 加 post-spike normalization 让 quantal output 也能自适应（架构改动，最后选项）。

日志：`/tmp/claude-1000/.../tasks/bhqg55vpg.output`（4090 拉回本地 cache）；命令记录在 commit `de2005e` 的脚本 `scripts/v4_diag_nan.py`。

### §3.5 supra MAL 最终结果（截至 step 800）
4090 supra / supra+AHP 继续在 MAL 下跑（截至记录时 step 800，未完成）:

| 步 | supra loss | supra hidden μ | supra+AHP loss | supra+AHP hidden μ |
|---|---|---|---|---|
| 0 | 11.80 | 0.303 | 11.80 | 0.274 |
| 200 | 5.55 | 0.241 | 5.74 | 0.231 |
| 400 | 5.69 | 0.202 | 6.42 | 0.207 |
| 600 | **3.97** | 0.196 | 4.64 | 0.191 |
| 800 | 4.58 | 0.181 | 4.67 | 0.178 |

**vs Adam baseline 最终（1500 步）**：supra Adam 7.33 vs MAL @ 800 = 4.58 → MAL 跑半程就比 Adam 全程低 ~2.7 nat。
→ **Lion 对 supra 的稀疏 SNN 梯度有显著帮助**。

---

## §4 模型尺寸效应（重要观察）
小模型（D=384 / 8 层）quantal 原样训练正常（hidden 0.22→0.32）；大模型（D=512 / 12 层）quantal hidden 起步就 0.006、塌到 0。

**原因（理论）**：quantal output = v_th·s ≈ 0.02; W_in (D=512) → DN=8192, kaiming `~N(0, 2/D)`. (W_in @ output) per-channel std = sqrt(2·E[output²]) = sqrt(2·0.02²·0.49) ≈ 0.02. 即 hidden 神经元的 V_pre 期望幅度 ~0.02，hidden v_th ≈ v_th_min + |...| ≈ 0.02-0.05。**V_pre ~ v_th 量级 → 发放率随机** → 略低于阈值时几乎不发放。

更深的层级联衰减：input_neuron 输出 ~0.02 → W_in → hidden V_pre ~0.02 → hidden 输出 ~0.02·firing_rate ≈ 0.006 → 下一层 W_in → ~0.006 → ...

→ quantal 在大模型下需要**初始化解决级联问题**（论文核心 contribution 之一）。

---

## §5 待写的章节
- §5 优化器分析（Muon vs Adam vs Lion sign-update 在稀疏 SNN 梯度下的行为；量化对比）
- §6 LSUV-style quantal 初始化（v_th = c·std(V_pre) 让 output 幅度最优）
- §7 NaN 失败模式定位（diag 脚本结果）+ 修复
- §8 完整 ablation：optimizer × spike_mode × AHP × init —— 推荐最终配置
- §9 与 V3 (supra) 对比：长训练（≥10k 步、≥1B token）下 quantal+MAL+LSUV 是否真的优

---

## 附录 A: commit / 数据 索引
- `255d1ff`：quantal + AHP 实现 + 配置 + Triton kernel
- `1e6845e`：ablation 脚本 + binned 数据支持
- `a6e3376`：Muon+Adam+Lion (MAL) 优化器
- `57b0783`：v_th/ahp clamp ≥ 1e-4
- `0a50df8`：LSUV scaffold (WIP)
- `de2005e`：NaN diag 脚本
- 4090 日志: `/workspace/abl_*.log` (Adam baseline), `/workspace/abl2_*.log` (MAL)
- 探针: `/tmp/v4_quantal_vth_probe.py`（已删，结果在 §3.2）
- 测试: `tests/test_segmented_plif.py` (12 pass)、`tests/test_v4_end_to_end.py` (2 pass)
